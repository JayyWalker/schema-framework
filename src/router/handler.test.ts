import { describe, it } from 'vitest'

describe('route handler', () => {
  it.todo('should call handler function from the provided route')

  it.todo('should throw error if the handler dose not create a response')
})
